apiVersion: v1
kind: Service

metadata:
  name: java-webapp-service

spec:
  type: NodePort

  selector:
    app: java-webapp

  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080